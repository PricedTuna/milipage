import { createTheme } from "@mui/material/styles";

const THEME = createTheme({
  palette: {
    primary: {
      main: "#0e64a3", // Azul por defecto
    },
    secondary: {
      main: "#dac355", // Tu color personalizado
    },
  },
});

export default THEME;
